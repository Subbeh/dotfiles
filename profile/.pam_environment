HOME                DEFAULT=/home/@{PAM_USER}
XDG_CONFIG_HOME     DEFAULT=${HOME}/.config
XDG_CACHE_HOME      DEFAULT=${HOME}/.cache
XDG_DATA_HOME       DEFAULT=${HOME}/.local/share

PATH                DEFAULT=${HOME}/.local/bin:/usr/bin:${PATH}
EDITOR              DEFAULT=vim
TERMINAL            DEFAULT=termite
