# dotfiles

1. install `stow`


2. clone repostitory

````
git clone --recurse-submodule git@github.com:Subbeh/dotfiles.git ~/.dotfiles
````

3. run `dot` to synchronize files
```
$HOME/.dotfiles/dot load -g -p
```
